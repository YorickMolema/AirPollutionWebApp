import { ComponentMeasurements } from './component-measurements';

describe('ComponentMeasurements', () => {
  it('should create an instance', () => {
    expect(new ComponentMeasurements()).toBeTruthy();
  });
});
